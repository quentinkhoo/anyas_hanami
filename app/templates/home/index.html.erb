<%# app/templates/web/home/index.html.erb %>
<h1>Safe Command Runner 👨‍💻</h1>
<p>
  Enter a directory path to list its contents using the <code>ls -la</code> command.
  For example: <code>.</code> or <code>config</code>
</p>

<form action="/" method="POST" style="display: flex; flex-direction: column; gap: 1rem; max-width: 600px;">
  <div>
    <label for="user_input">Directory Path:</label>
    <input
      type="text"
      id="user_input"
      name="user_input"
      value="<%= user_input %>"
      style="width: 100%; font-family: monospace;"
    >
  </div>

  <button type="submit">Run Command</button>

  <div>
    <label for="output">Output:</label>
    <textarea
      id="output"
      readonly
      rows="15"
      style="width: 100%; background: #f4f4f4; font-family: monospace;"
    ><%= output %></textarea>
  </div>
</form>